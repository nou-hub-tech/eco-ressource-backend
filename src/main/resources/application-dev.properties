# Profil dev du module annonces.
# Si PERSPECTIVE_API_KEY existe, la moderation Perspective est active automatiquement.
perspective.enabled=auto

# EmailJS est pret au demarrage avec les valeurs du template configurees dans application.properties.
moderation.admin-email=${MODERATION_ADMIN_EMAIL:admin@marketplace.com}
